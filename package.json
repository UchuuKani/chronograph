{
  "name": "chronograph",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "tsc && node ./dist/index.js",
    "seed": "psql -d chronograph -a -f ./seed.sql",
    "queryOne": "psql -d chronograph -a -f ./src/dbQueries/query1.sql",
    "queryTwo":"psql -d chronograph -a -f ./src/dbQueries/query2.sql"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "typescript": "^4.3.2"
  }
}
